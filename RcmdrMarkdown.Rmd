<!-- R Commander Markdown Template -->

Replace with Main Title
=======================

### Your Name

### `r as.character(Sys.Date())`

```{r echo=FALSE}
# include this code chunk as-is to set options
knitr::opts_chunk$set(comment=NA, prompt=TRUE, out.width=750, fig.height=8, fig.width=8)
library(Rcmdr)
library(car)
library(RcmdrMisc)
```

```{r}
data(mtcars)
```

```{r}
with(mtcars, Hist(cyl, scale="frequency", breaks="Sturges", col="darkgray"))
```

```{r}
scatterplot(drat~cyl, reg.line=FALSE, smooth=FALSE, spread=FALSE, 
  boxplots=FALSE, span=0.5, ellipse=FALSE, levels=c(.5, .9), data=mtcars)
```

```{r}
with(mtcars, lineplot(disp, drat))
```

```{r}
library(rgl, pos=17)
```

```{r}
library(nlme, pos=18)
```

```{r}
library(mgcv, pos=18)
```


